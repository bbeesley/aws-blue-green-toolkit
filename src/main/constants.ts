export enum ClusterState {
  STARTING = 'starting',
  STOPPING = 'stopping',
  STOPPED = 'stopped',
  AVAILABLE = 'available',
}

export enum DatabaseReference {
  a = 'a',
  b = 'b',
}

export enum Operation {
  ENABLE,
  DISABLE,
}

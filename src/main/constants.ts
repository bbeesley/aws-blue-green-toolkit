export enum ClusterState {
  STARTING = 'starting',
  STOPPING = 'stopping',
  STOPPED = 'stopped',
  AVAILABLE = 'available',
}

export enum StackReference {
  a = 'a',
  b = 'b',
}

export enum Operation {
  ENABLE,
  DISABLE,
}
